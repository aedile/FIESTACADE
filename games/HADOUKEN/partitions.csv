# Name,   Type, SubType, Offset,    Size,     Flags
# Only used when HADOUKEN is built and flashed on its own. Under MINIMAME the launcher's
# table is the one on the device: this image goes in a game slot and the clip in a data
# partition the launcher lays out next to it (see games.toml, `data_kb`).
nvs,      data, nvs,     0x9000,    0x6000,
phy_init, data, phy,     0xf000,    0x1000,
factory,  app,  factory, 0x10000,   0x80000,
media,    data, 0x40,    0x90000,   0x480000,
